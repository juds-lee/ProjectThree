


const PokemonCard = (props) => {
    // const attacks = [];
    // props.handleAbility()
    // props.sprites.map((i) => {
    //   props.setAbilities(i.ability.name)
    // })
    return(
        <div></div>
    )
}

       

export default PokemonCard;